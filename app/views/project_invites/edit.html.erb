<%= simple_form_for [@project, @project_invite] do |f| %>
  <%= f.input :message %>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
<%= link_to "Delete", project_invite_path(@project_invite), method: :delete%>
